# =============================================================================
# VetAI Consultant - Docker Environment Variables
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# AI Model Selection
# Options: gemini (default), glm
# -----------------------------------------------------------------------------
AI_MODEL=glm

# -----------------------------------------------------------------------------
# GLM API Configuration (ZhipuAI / Z.ai)
# Required when AI_MODEL=glm
# Get your key from: https://open.bigmodel.cn/ or https://z.ai/
# -----------------------------------------------------------------------------
GLM_API_KEY=your_glm_api_key_here
GLM_API_URL=https://api.z.ai/api/anthropic
GLM_MODEL=glm-4.7

# GLM Embedding Configuration
GLM_EMBEDDING_URL=https://api.z.ai/api/v1/embeddings
GLM_EMBEDDING_MODEL=glm-4.7

# -----------------------------------------------------------------------------
# Gemini API Configuration (Google)
# Required when AI_MODEL=gemini
# Get your key from: https://makersuite.google.com/app/apikey
# -----------------------------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------------------------
# Legacy API Key (maps to GLM_API_KEY for backward compatibility)
# -----------------------------------------------------------------------------
API_KEY=your_glm_api_key_here

# -----------------------------------------------------------------------------
# Supabase Configuration
# For local development: run `supabase start` and use the output values
# For production: create a project at https://supabase.com
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# -----------------------------------------------------------------------------
# OPTIONAL: Port Configuration
# Defaults shown - change if you have conflicts
# -----------------------------------------------------------------------------
# FRONTEND_PORT=80
# BACKEND_PORT=8000
# QDRANT_PORT=6333

# -----------------------------------------------------------------------------
# Docker Compose Notes
# -----------------------------------------------------------------------------
# - The .env file should be in the same directory as docker-compose.yml
# - Do not commit .env to version control (it's in .gitignore)
# - All services run on a Docker internal network (vetai-network)
# - Data persists in Docker volumes even after container restart
# - For Supabase local dev, run `npx supabase start` separately
# -----------------------------------------------------------------------------
